# 운영체제 11번째 과제 - 파일시스템 이해하기

## 복합문제 1번 - FAT 파일시스템 exam.txt

### (1) exam.txt 디스크 블록 순서 (첫 번째 블록: 53번)
FAT 테이블 추적: 53 -> 2 -> 124 -> 54 -> 320 -> -1 (EOF)

### (2) 블록 크기 1KB일 때 exam.txt 파일 크기로 가장 적절한 것
5개 블록 사용 (4KB < 파일크기 <= 5KB)
정답: 4.5KB

---

## 복합문제 2번 - FAT 파일시스템 score.txt

### (1) score.txt 디스크 블록 순서 (첫 번째 블록: 55번)
FAT 테이블 추적: 55 -> 321 -> 52 -> 123 -> -1 (EOF)

### (2) 블록 크기 2KB일 때 score.txt 파일 크기로 가장 적절한 것
4개 블록 사용 (6KB < 파일크기 <= 8KB)
정답: 7KB

---

## 복합문제 3번 - 유닉스 파일시스템 /usr/dev/source/app.c 읽기

### (1) 읽어야 하는 i-node 수와 순서
총 5개의 i-node를 순서대로 읽어야 한다.
1. / (루트 디렉터리) i-node
2. 2. /usr 디렉터리 i-node
   3. 3. /usr/dev 디렉터리 i-node
      4. 4. /usr/dev/source 디렉터리 i-node
         5. 5. /usr/dev/source/app.c 파일의 i-node
           
            6. ### (2) i-node와 디스크 블록이 읽혀지는 순서
            7. 1. 루트 i-node
               2. 2. 루트 디렉터리 블록들
                  3. 3. /usr의 i-node
                     4. 4. /usr 디렉터리 블록
                        5. 5. /usr/dev의 i-node
                           6. 6. /usr/dev 디렉터리 블록
                              7. 7. /usr/dev/source의 i-node
                                 8. 8. /usr/dev/source 디렉터리 블록
                                    9. 9. /usr/dev/source/app.c의 i-node
                                       10. 10. /usr/dev/source/app.c의 파일 블록
                                          
                                           11. ### (3) i-node 테이블을 메모리에 적재해 두는 이유
                                           12. i-node에는 파일 크기, 파일 블록의 위치 정보 등 파일 메타 정보가 들어있다.
                                           13. 파일 읽기나 쓰기가 진행되면 파일 메타 정보가 계속 액세스된다.
                                           14. 만일 i-node를 디스크에 둔 상태로 파일 읽기가 진행되면, 파일 블록뿐 아니라 i-node도 반복하여 읽게 되므로,
                                           15. 파일을 열 때 메모리의 i-node 테이블에 디스크 i-node를 적재해두는 것이 효과적이다.
